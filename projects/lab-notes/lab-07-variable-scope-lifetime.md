Final takeaway

The big lesson across all six exercises is this:

scope rules explain where values come from
closures explain when values are captured
deterministic cleanup explains when resources are released
avoiding globals makes dependencies explicit
explicit dependencies make systems much easier to test and maintain

Paste your trade_pipeline.py and I’ll do Exercises 4 and 5 against the real file.